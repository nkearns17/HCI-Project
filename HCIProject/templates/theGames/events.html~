<html>
<body>

{% extends 'theGames/base-bootstrap.html' %}

{% block content %}

	<h2>Events</h2>	
	<br><br>

	<div class="btn-toolbar" role="toolbar">
	{% if event_list %}
		<UL>
		{% for event in event_list %}
		        <button type="button" class="btn btn-lg">{{ event.name }}</button>
		{% endfor %}
		</UL>
	{% else %}
		<P>No Events at present.</P>
	{% endif %}
	</div> 

{% endblock %}

</body>
</html>
